/**
 * Promises for picture/video of the day and related support functions.
 */

'use strict';

function featuredImagePromise(app, req) {}

function featuredVideoPromise(app, req) {}

module.exports = {
   featuredImagePromise: featuredImagePromise,
   featuredVideoPromise: featuredVideoPromise
};